apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: optimizer-backup
  namespace: intelligent-optimizer-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
