apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - workloads-namespace.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - optimizerconfig-workloads.yaml
  # servicemonitor.yaml requires Prometheus Operator CRDs — omitted until installed

images:
  - name: us-central1-docker.pkg.dev/k8s-resource-optimizer/optimizer/controller
    newTag: latest

commonLabels:
  app.kubernetes.io/part-of: intelligent-cluster-optimizer
  app.kubernetes.io/managed-by: kustomize
